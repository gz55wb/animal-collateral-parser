# Animal Scraper Requirements
# Production-level dependencies for professional Python application

# Core dependencies
requests>=2.31.0
beautifulsoup4>=4.12.0
aiohttp>=3.9.0
pydantic<2.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Optional: Development dependencies
# black>=23.0.0        # Code formatting
# flake8>=6.0.0        # Linting  
# mypy>=1.7.0          # Type checking
# isort>=5.12.0        # Import sorting

# System dependencies (install separately if needed):
# - Python 3.8+ required
# - asyncio (built-in)
# - pathlib (built-in)
# - logging (built-in)
# - time (built-in)
# - re (built-in)
# - hashlib (built-in)